# CRE NeuroProof workflow

Post-action validation only. See `workflow.ts`.

```bash
# After local LoomGuard emit:
# 1) evidence bundle written under receipt-bundles/
# 2) validator-api /v1/replay
# 3) this workflow records ValidationResult for onchain anchor
```
