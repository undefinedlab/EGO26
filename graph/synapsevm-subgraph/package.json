{
  "name": "synapsevm-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy synapsevm --node https://api.studio.thegraph.com/deploy/ --version-label v0.1.0",
    "create-local": "graph create --node http://localhost:8020/ synapsevm",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 synapsevm"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.95.0",
    "@graphprotocol/graph-ts": "^0.37.0"
  }
}
